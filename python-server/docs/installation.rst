============
Installation
============

At the command line::

    $ easy_install python-server

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python-server
    $ pip install python-server
